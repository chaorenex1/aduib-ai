name: delete_memory
description: "Delete a memory record (MemoryRecord) and clean up associated MemoryTopicAssociation. Does NOT delete the linked KnowledgeDocument to avoid RAG index residual issues."
parameters:
  type: object
  properties:
    memory_id:
      type: string
      description: The memory record ID to delete
  required: ["memory_id"]
configs: {}
icon: "\U0001F5D1"
provider: builtin_provider
type: builtin
credentials: none
side_effect: true

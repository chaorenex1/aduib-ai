name: createMemory
description: Create short-term memory for the agent
parameters:
  type: object
  properties:
    content:
      type: string
      description: Memory content to store
    short_term:
      type: boolean
      description: "Whether the memory is short-term (default: true)"
  required: ["content", "short_term"]
configs: {}
icon: "🧠"
provider: "builtin_provider"
type: builtin
credentials: none
side_effect: true

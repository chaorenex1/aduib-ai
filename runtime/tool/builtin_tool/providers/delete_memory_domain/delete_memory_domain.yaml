name: delete_memory_domain
description: Delete a memory domain (MemoryBase record). Checks ownership and warns about associated MemoryRecords.
parameters:
  type: object
  properties:
    memory_base_id:
      type: string
      description: The MemoryBase UUID to delete
    user_id:
      type: string
      description: The user ID for ownership verification
  required: ["memory_base_id", "user_id"]
configs: {}
icon: "🗑️"
provider: builtin_provider
type: builtin
credentials: none
side_effect: true

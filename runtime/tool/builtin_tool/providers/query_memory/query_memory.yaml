name: queryMemory
description: Query agent memory
parameters:
  type: object
  properties:
    query:
      type: string
      description: Memory search query
    short_term:
      type: boolean
      description: "Whether the memory is short-term (default: false)"
  required: ["query", "short_term"]
configs: {}
icon: "🧠"
provider: "builtin_provider"
type: builtin
credentials: none
side_effect: false

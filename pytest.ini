[pytest]
# Some environments/plugins may override discovery via pyproject.toml.
# We pin discovery rules here so regression gates consistently run.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra

